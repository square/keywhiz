<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.squareup.keywhiz</groupId>
    <artifactId>keywhiz-parent</artifactId>
    <version>0.7.4-SNAPSHOT</version>
  </parent>

  <artifactId>keywhiz-model</artifactId>
  <name>Keywhiz Model</name>

  <dependencies>
    <dependency>
      <groupId>org.jooq</groupId>
      <artifactId>jooq</artifactId>
    </dependency>
    <dependency>
      <groupId>org.jooq</groupId>
      <artifactId>jooq-meta</artifactId>
    </dependency>
    <dependency>
      <groupId>org.jooq</groupId>
      <artifactId>jooq-codegen</artifactId>
    </dependency>

    <dependency>
      <groupId>mysql</groupId>
      <artifactId>mysql-connector-java</artifactId>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- Creates (or re-creates) a keywhizdb_test mysql database. -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>sql-maven-plugin</artifactId>
        <dependencies>
          <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
            <version>5.1.35</version>
          </dependency>
        </dependencies>

        <executions>
          <execution>
            <phase>initialize</phase>
            <goals>
              <goal>execute</goal>
            </goals>
          </execution>
        </executions>

        <configuration>
          <driver>com.mysql.jdbc.Driver</driver>
          <url>jdbc:mysql://localhost/?useUnicode=true&amp;characterEncoding=utf8</url>
          <username>root</username>
          <sqlCommand>
              DROP DATABASE IF EXISTS keywhizdb_test;
              CREATE DATABASE keywhizdb_test;
          </sqlCommand>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.flywaydb</groupId>
        <artifactId>flyway-maven-plugin</artifactId>

        <executions>
          <execution>
            <phase>generate-sources</phase>
            <goals>
              <goal>migrate</goal>
            </goals>
          </execution>
        </executions>

        <configuration>
          <url>jdbc:mysql://localhost/keywhizdb_test?useUnicode=true&amp;characterEncoding=utf8</url>
          <user>root</user>
          <locations>
            <location>filesystem:../server/src/main/resources/db/mysql/migration</location>
          </locations>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.jooq</groupId>
        <artifactId>jooq-codegen-maven</artifactId>

        <!-- The plugin should hook into the generate goal -->
        <executions>
          <execution>
            <phase>generate-sources</phase>
            <goals>
              <goal>generate</goal>
            </goals>
          </execution>
        </executions>

        <configuration>
          <jdbc>
            <driver>com.mysql.jdbc.Driver</driver>
            <url>jdbc:mysql://localhost/keywhizdb_test?useUnicode=true&amp;characterEncoding=utf8</url>
            <user>root</user>
          </jdbc>

          <!-- Generator parameters -->
          <generator>
            <name>org.jooq.util.JavaGenerator</name>
            <database>
              <name>org.jooq.util.mysql.MySQLDatabase</name>
              <includes>.*</includes>
              <excludes>mysql.*</excludes>
              <inputSchema>keywhizdb_test</inputSchema>

              <!-- Registers custom types -->
              <customTypes>
                <customType>
                  <name>OffsetDateTime</name>
                  <type>java.time.OffsetDateTime</type>
                  <converter>keywhiz.model.OffsetDateTimeConverter</converter>
                </customType>
                <customType>
                  <name>java.lang.Boolean</name>
                  <converter>keywhiz.model.TinyIntConverter</converter>
                </customType>
              </customTypes>

              <!-- Forces JOOQ to use custom types in generated code -->
              <forcedTypes>
                <forcedType>
                  <name>OffsetDateTime</name>
                  <expression>.*\.createdat|.*\.updatedat|.*\.created_at|.*\.updated_at</expression>
                  <types>timestamp.*</types>
                </forcedType>
                <forcedType>
                  <name>java.lang.Boolean</name>
                  <expression>.*\.enabled|.*\.automationallowed</expression>
                  <types>tinyint.*</types>
                </forcedType>
              </forcedTypes>
            </database>
            <target>
              <packageName>keywhiz.jooq</packageName>
            </target>
          </generator>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
